// ABOUTME: Small brand wordmark ("§ clekeeper") — a quiet web identity anchor shown top-left,
// ABOUTME: opposite the account pill, on the dashboard and the first-run screen.
export function Wordmark() {
  return (
    <span className="brand">
      <span className="mk" aria-hidden="true">§</span> clekeeper
    </span>
  )
}
